const { PrismaClient } = require("@prisma/client"); const client = new PrismaClient(); async function main(){ try { const user = await client.$queryRawUnsafe("SHOW CREATE TABLE users"); const plan = await client.$queryRawUnsafe("SHOW CREATE TABLE savings_plans"); console.log(JSON.stringify({ user, plan }, null, 2)); } catch (e) { console.error(e); process.exit(1); } finally { await client.$disconnect(); } } main();
